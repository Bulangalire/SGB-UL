{% extends 'base.html.twig' %}
{% form_theme formrubrique 'bootstrap_4_layout.html.twig' %}
{% block body %}

    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }
        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Adminitration SGB-UL!</h1>
        <h3>
            Connecter :
            {{ app.user.nom }} : {{ app.user.services.designation }} 
            ✅</h3>

        Choississez la configuration à faire
        <ul>
            <li>Ajouter un utilisateur :<a href="{{path('useradd')}}">
                    Nouveau</a>
            </li>

        </ul>

        <ul>
            <li> 
           {{ form_start(formrubrique)}}
           {{ form_widget(formrubrique)}}
            <button type="submit" class="btn btn-success">Importer</button>

            {{ form_end(formrubrique)}}
            </li>
        </ul>

      

    </div>
{% endblock %}